{
  "buildCommand": "pnpm --filter @open-slide/core build && pnpm --filter demo build",
  "outputDirectory": "apps/demo/dist",
  "installCommand": "pnpm install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
